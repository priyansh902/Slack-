spring:
  application:
    name: Slack
  
  file.upload-dir: /uploads/resumes

  servlet:
      multipart:
        max-file-size: 10MB
        max-request-size: 10MB

  

  profiles:
    active:
    - dev
  


